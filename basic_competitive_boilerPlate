#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define pii pair<int, int>
#define pll pair<long long, long long>
#define vi vector<int>
#define vvi vector<vi>
#define vll vector<long long>
#define mii map<int, int>
#define MOD 1000000007
#define PI 3.1415926535897932384626433832795

/* FUNCTIONS */
#define rep(i,s,e) for(long long int i=s;i<=e;i++) //complete for
#define rrep(i,e,s) for(long long int i=e;i>=s;i--) //reverse for

template <class T>
void print_v(vector<T> &v) { for (auto x : v) cout << x << ' '; cout<<endl; }

void yes() { cout<<"YES\n"; }
void no() { cout<<"NO\n"; }


template <class T>
vector<T> read_stream(int n) {
    vector<T> res(n);
    for (int i = 0; i < n; ++i) {
        cin >> res[i];
    }
    return res;
 }

template <class T>
vector<vector<T>> read_matrix(int n, int m) {
    vector<vector<T>> res(n);
    for (int i = 0; i < n; ++i) {
        res[i] = read_stream<T>(m);
    }
    return res;
 }




void solve(){
    int n;
    cin>>n;

}



int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    #ifndef ONLINE_JUDGE
        freopen("input.txt", "r", stdin);
        freopen("output.txt", "w", stdout);
    #endif

    #ifndef ONLINE_JUDGE
        freopen("Error.txt", "w", stderr);
    #endif

    int tc;
    cin >> tc;
    while(tc--){
        solve();
    }

    return 0;
}
